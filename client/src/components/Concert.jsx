import React from 'react';

const Concert = (props) => {
  return (
    <div>
      { props.tour_name }
      { props.event_date }
      { props.venue }
      { location }
    </div>
  )
};

export default Concert;